<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="role="main" class="container"">
    <div class="row">
      <div class="col-md-4">
      </div>
      <div class="col-md-4 blog-main">
        <div class="form-group">
          <%= form.label :title %>
          <%= form.text_field :title, id: :blog_title, class: 'form-control' %>
          <%= form.label :body %>
          <%= form.text_area :body, id: :blog_body, class: 'form-control' %>
          <%= form.submit "Submit", class: 'btn btn-outline-primary' %>
          <%= link_to 'Back', blogs_path, class: 'btn btn-outline-secondary' %>
        </div>
      </div>
      <div class="col-md-4">
      </div>
    </div>
  </div>
<% end %>
